[build]
builder = "nixpacks"

[build.nixpacks]
node = "20"
buildCommands = ["cd backend && npm ci && cd ..", "echo 'Backend dependencies installed'"]

[deploy]
startCommand = "node backend/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
